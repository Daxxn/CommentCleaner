<Window x:Class="CommentCleanerWPF.FolderDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommentCleanerWPF"
		xmlns:controls="clr-namespace:CommentCleanerWPF.Controls"
		xmlns:folder="clr-namespace:CommentCleanerWPF.Models.Folder"
        mc:Ignorable="d"
        Title="FolderDialogView" Height="450" Width="400">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<TreeView x:Name="FolderTree" ItemsSource="{Binding Path=BaseFolder.Children}">
			<TreeView.ItemTemplate>
				<DataTemplate>
					<StackPanel>
					</StackPanel>
				</DataTemplate>
			</TreeView.ItemTemplate>
		</TreeView>
    </Grid>
</Window>
