<UserControl x:Class="CommentCleanerWPF.Controls.FolderItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommentCleanerWPF.Controls"
			 xmlns:controls="clr-namespace:CommentCleanerWPF.Controls"
			 x:Name="root"
             mc:Ignorable="d">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="{Binding Folder.Name, ElementName=root}"/>
		<Border Grid.Row="1">
			<TreeView ItemsSource="{Binding Folder.Children, ElementName=root}">
				<TreeView.ItemTemplate>
					<DataTemplate>
                        <local:FolderItem/>
                    </DataTemplate>
				</TreeView.ItemTemplate>
			</TreeView>
		</Border>
	</Grid>
</UserControl>
